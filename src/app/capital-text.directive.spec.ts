import { CapitalTextDirective } from './capital-text.directive';

describe('CapitalTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalTextDirective();
    expect(directive).toBeTruthy();
  });
});
